before_install:
  - sdk install java 19.0.2-open
  - sdk use java 19.0.2-open

install:
  - ./gradlew :flexible-adapter:publishToMavenLocal -Padapter -PskipSigning=true -Pgroup=$GROUP -Pversion=$VERSION
